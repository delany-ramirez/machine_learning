# Respaldo pip para quien no pueda usar uv (ver docs/guia-entorno.md, opción B).
# La fuente de verdad es pyproject.toml (+ uv.lock con versiones exactas); esta lista replica
# sus dependencias con versiones mínimas y hay que mantenerla en sincronía a mano.
#
#   python -m venv .venv
#   # Windows:  .venv\Scripts\activate     Linux/macOS:  source .venv/bin/activate
#   pip install -r requirements.txt
#   python -m ipykernel install --user --name ml-curso --display-name "Python (ml-curso)"

# --- Núcleo científico ---
numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5
statsmodels>=0.14

# --- Visualización ---
matplotlib>=3.8
seaborn>=0.13

# --- Entorno de trabajo ---
jupyterlab>=4.2
ipykernel>=6.29
ipywidgets>=8.1

# --- Recolección de datos (módulo 2) ---
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.2

# --- Ensambles e interpretabilidad (módulo 4) ---
xgboost>=2.1
lightgbm>=4.5
imbalanced-learn>=0.12
shap>=0.46

# --- Optimización de hiperparámetros (módulo 3) ---
optuna>=3.6

# --- Reducción de dimensionalidad (módulo 5) ---
umap-learn>=0.5

# --- Deep learning (módulo 5) ---
# En Linux, para la variante CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.4

# --- MLOps (módulos 2 y 6) ---
mlflow>=2.16
fastapi>=0.115
uvicorn>=0.30
dvc>=3.55

# --- Datasets ---
ucimlrepo>=0.0.7
